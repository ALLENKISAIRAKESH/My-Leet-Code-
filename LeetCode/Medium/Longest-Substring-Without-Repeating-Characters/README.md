# Longest Substring Without Repeating Characters

| Field | Value |
|-------|-------|
| Platform | LeetCode |
| Difficulty | Medium |
| Language | python3 |
| URL | [https://leetcode.com/problems/longest-substring-without-repeating-characters/](https://leetcode.com/problems/longest-substring-without-repeating-characters/) |
| Submitted | 2026-08-29T12:03:13.246Z |


## Problem Statement

Given a string s, find the length of the longest substring without repeating characters.

## Solution

```python
class Solution:
    def lengthOfLongestSubstring(self, s: str) -> int:
        char_map = {}
        left = 0
        max_len = 0
        for right, char in enumerate(s):
            if char in char_map and char_map[char] >= left:
                left = char_map[char] + 1
            char_map[char] = right
            max_len = max(max_len, right - left + 1)
        return max_len
```

---

*Explanation generated by CodeSync — AI explanation will be added once the OpenAI service is enabled.*